{
  "name": "dawonweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "bun run --cwd web dev",
    "build": "bun run build:wasm && bun run --cwd web build",
    "build:wasm": "./scripts/build-wasm.sh",
    "preview": "bun run --cwd web preview",
    "dev:desktop": "cd desktop/src-tauri && cargo tauri dev",
    "build:desktop": "bun run build:wasm && bun run --cwd web build && cd desktop/src-tauri && cargo tauri build",
    "test": "bun run test:rust && bun run --cwd web test && bun run --cwd backend test",
    "test:rust": "cargo test --workspace",
    "lint": "cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --check",
    "size-check": "./scripts/size-check.sh",
    "clean": "cargo clean && rm -rf web/src/wasm web/dist"
  }
}
