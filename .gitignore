# --- Rust ---
/target
**/target
*.profraw
perf.data*
flamegraph.svg

# --- artefak WASM (hasil scripts/build-wasm.sh, jangan di-commit) ---
/web/src/wasm/

# --- Node / pnpm ---
node_modules/
.pnpm-store/
/web/dist/
/web/.vite/
*.tsbuildinfo

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
*.swp

# --- lokal ---
.env
.env.local
/tmp/
*.wav
!tests/fixtures/*.wav

# --- output deploy Vercel (dirakit scripts/vercel-build.sh) ---
/.vercel/
.vercel
.env*

# --- worktree Claude Code (folder kerja sementara, BUKAN isi repo) ---
# Pernah ter-`git add -A` sebagai gitlink di ff0cebc dan muncul selamanya
# sebagai "modified"; isinya sudah punya branch sendiri, tidak perlu dilacak.
.claude/worktrees/

# --- desktop (Tauri, docs/20) ---
# `gen/` berisi skema capability yang ditulis ulang tauri-build tiap compile;
# `target/` sudah tercakup di atas. Ikon di `desktop/src-tauri/icons/` SENGAJA
# dilacak: ia hasil `cargo tauri icon` yang dijalankan sekali, bukan tiap build.
/desktop/src-tauri/gen/
