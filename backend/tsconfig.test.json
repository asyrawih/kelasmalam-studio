{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* Harness tes berjalan di Node, bukan di Worker: `node:sqlite`, `node:fs`,
       dan `process` memang ada di sana. */
    "types": ["node"],
    "lib": ["ES2022", "WebWorker", "DOM.Iterable"]
  },
  "include": ["src/**/*.test.ts", "src/test-support"],
  "exclude": []
}
